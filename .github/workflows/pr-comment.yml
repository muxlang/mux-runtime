name: PR Report Comment
# Posts the Valgrind and A/B benchmark reports produced by the "Runtime Reports"
# workflow. The full benchmark suite in benchmarks.yml remains on demand and on
# release tags, so it produces no PR artifact. This separate workflow uses
# workflow_run to get a trusted base-repo write token even when the report run
# came from an untrusted fork PR.
#
# Threat model: a fork controls its own Runtime Reports workflow (pull_request
# runs the fork's copy), so EVERYTHING that run uploads is untrusted. This
# workflow therefore:
#   - resolves the target PR only from trusted workflow_run metadata (head repo
#     + branch + SHA), never from artifact data;
#   - consumes only the raw logs from the exact expected artifacts and does the
#     fence-escaping, length cap, and markdown assembly HERE (a fork cannot make
#     the report run skip escaping it does not perform);
#   - takes pass/fail from the trusted jobs API, not from the artifact.
# It never checks out or executes the PR's code.
#
# NOTE: workflow_run only fires for the copy of this file on the default branch,
# so it takes effect once merged to main (not from the PR that adds it).
on:
  workflow_run:
    workflows: ["Runtime Reports"]
    types: [completed]

# Serialize report runs for the same PR branch. The sticky-comment update is a
# lookup-then-create: two report runs completing close together would both find
# no marker and both post, producing exactly the duplicate comments the marker
# exists to prevent. GitHub offers no atomic comment upsert, so serializing per
# PR is what makes the pair safe. Keyed on the trusted workflow_run head fields
# because the PR number is not resolved until a step below.
#
# cancel-in-progress is false so an in-flight post is never killed midway. A
# queued run can still be superseded by a newer one, which is correct here: the
# newest report is the one worth posting.
concurrency:
  group: ${{ github.workflow }}-${{ github.event.workflow_run.head_repository.full_name }}-${{ github.event.workflow_run.head_branch }}
  cancel-in-progress: false

permissions:
  contents: read
  actions: read # download artifacts + read job conclusions from the run
  pull-requests: write # post the comment

jobs:
  comment:
    # Only PR-triggered report runs carry a PR to comment on (skip push to main).
    if: ${{ github.event.workflow_run.event == 'pull_request' }}
    runs-on: ubuntu-latest
    steps:
      # Checks out this workflow's OWN ref (main - workflow_run always runs
      # the copy on the default branch, never the triggering PR's), not the
      # untrusted PR's code, so this stays consistent with the threat model
      # above. Needed only so the two scripts below exist on disk to run;
      # the original inline-shell version of this workflow needed no
      # checkout at all, which is why this was missing until caught here.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Resolve target PR from trusted metadata
        id: pr
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
          HEAD_OWNER: ${{ github.event.workflow_run.head_repository.owner.login }}
          HEAD_BRANCH: ${{ github.event.workflow_run.head_branch }}
        run: ./scripts/ci/resolve-target-pr.sh
      - name: Download report artifacts from the triggering run
        if: ${{ steps.pr.outputs.number != '' }}
        continue-on-error: true # a path-skipped report run uploads no artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          pattern: pr-comment-*
          path: reports
      - name: Assemble and post comment
        if: ${{ steps.pr.outputs.number != '' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          PR: ${{ steps.pr.outputs.number }}
          RUN_ID: ${{ github.event.workflow_run.id }}
          HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
          RUN_URL: ${{ github.event.workflow_run.html_url }}
        run: ./scripts/ci/post-report-comment.sh
