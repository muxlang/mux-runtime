# Mux Runtime

The runtime library crate published as `mux-runtime`. The compiler links against it when producing executables.

**For full documentation, see the [main README](https://github.com/DerekCorniello/mux-lang#readme).**